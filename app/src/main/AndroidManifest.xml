<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.k0d4black.theforce">

    <application
        android:name=".TheForceApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme">

        <activity android:name=".shared.android.base.BaseActivity" />

        <activity
            android:name=".feature.home.ui.HomeActivity"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".feature.charactersearchresults.ui.CharacterSearchResultsActivity"
            android:parentActivityName=".feature.home.ui.HomeActivity" />

        <activity
            android:name=".feature.characterdetails.ui.CharacterDetailsActivity"
            android:parentActivityName=".feature.home.ui.HomeActivity" />

        <activity
            android:name=".feature.about.ui.AboutActivity"
            android:parentActivityName=".feature.home.ui.HomeActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="${crashlyticsEnabled}" />
    </application>

</manifest>